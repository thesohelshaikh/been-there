## 1. UI Implementation

- [x] 1.1 Add `tripToDelete` and `showingDeleteAlert` state to `TripsView`
- [x] 1.2 Replace `.onDelete` with custom trailing swipe actions in `TripsView`
- [x] 1.3 Add confirmation alert for deletion in `TripsView`
