## 1. Logic Updates

- [x] 1.1 Implement `updateTrip(_ trip: Trip)` in `TripManager`
- [x] 1.2 Add unit test for `updateTrip` in `TripManagerTests`

## 2. UI Updates

- [x] 2.1 Update `AddTripSheet` to accept an optional `tripToEdit: Trip?` and initialize state
- [x] 2.2 Update `AddTripSheet` title and button labels based on mode (Add vs Edit)
- [x] 2.3 Update `TripsView` to track a `tripToEdit` and present `AddTripSheet` for editing
- [x] 2.4 Add swipe-to-edit action to `TripRow` in `TripsView`
