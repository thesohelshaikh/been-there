## 1. Manager Robustness

- [x] 1.1 Update `TripManager.save()` with `do-catch` and logging
- [x] 1.2 Update `TripManager.load()` with `do-catch` and logging
- [x] 1.3 Implement `clearAllTrips()` in `TripManager`

## 2. Settings Integration

- [x] 2.1 Update `SettingsView` to accept `tripManager`
- [x] 2.2 Add "Clear All Trips" button to `SettingsView` with confirmation
- [x] 2.3 Ensure `ContentView` passes `tripManager` to `SettingsView`
